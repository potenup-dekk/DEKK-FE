/**
 * NextAuth Route Handler (App Router)
 * TODO:
 * 1) NextAuth handler 생성
 * 2) authOptions import 연결
 * 3) handler를 GET/POST로 export
 *
 * Note:
 * - OAuth Redirect URI는 /api/auth/callback/{provider} 로 매칭됨
 */
